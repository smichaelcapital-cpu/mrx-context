# B1.7.4 — Arbitration Cover Layout — RECON ONLY

**Date:** 2026-05-16
**Author:** Opus (architect)
**Builder:** Sonnet #1 (Lane B)
**Branch:** feature/b1.7.4-recon (cut from origin/main after git fetch)

---

## Header (per RULE_SHEET_v1)

- Universal? YES (cover layout variant; serves all arbitration matters)
- Code or prompt? RECON — no code, no prompt changes this pass
- File location: N/A (read-only)
- Commit prefix: docs: (recon report is a markdown capture, not code)

---

## Purpose

Produce a complete recon capture for the Olsen arbitration cover layout problem. NO CODE CHANGES THIS PASS. The deliverable is a markdown knowledge capture that becomes the input to the B1.7.4 build spec written by Opus tomorrow.

Why recon-only: B1.7.4 may also affect appearances page anchoring for arbitration (per renderer gaps capture). Scope is structurally larger than B1.7.3 and warrants a separate spec written from a complete recon, not a fast guess.

---

## RULE-RECON-FIRST — Mandatory

Builder produces ONE deliverable: a recon capture saved to repo. Save path:

`mrx-context/knowledge/2026-05-16_OLSEN_ARBITRATION_COVER_RECON.md`

The capture must contain the sections below in order.

---

## Section 1 — Branch verification

- Run `git fetch origin main`
- Run `git checkout -b feature/b1.7.4-recon origin/main` in mrx-context AND in mrx_engine_v1.
- Run `git branch --show-current` in both. Paste both outputs.
- Run `git log origin/main..HEAD --oneline` in both. Both must be empty.

---

## Section 2 — Olsen oracle pages 1-2 verbatim

Read the Olsen oracle FINAL at:
`C:\Users\scott\OneDrive\Documents\mrx_engine_v1\mrx_engine_v1\io\analysis\032025olsen\<oracle path>`

If exact oracle filename is unclear, ask Opus before reading. Paste pages 1-2 verbatim into the capture. These are the cover + appearances pages.

NOTE: Olsen oracle FINAL is NOT public-repo content per Rule 16 — but quoting structural layout (header counts, slot positions, line-by-line shape) into the recon doc is allowed. DO NOT paste full client/witness names or sensitive identifying details into the public mrx-context repo capture. Use placeholders: `[CLAIMANT]`, `[RESPONDENT]`, `[WITNESS]`, `[FIRM_A]`. Capture STRUCTURE, not PII.

If unclear what counts as PII vs structure, ask Opus before pasting.

---

## Section 3 — Current cover renderer code

Read `src/stage5/front_matter/cover.py`. Paste:

- The full `_DEPO_START_IDX` lookup table.
- The current 2-line and 3-line court_header_lines code paths.
- The exact line where count=1 raises NotImplementedError.
- Any helper functions called by the cover layout logic (signatures only, not full bodies, unless short).

---

## Section 4 — Olsen cover.json

Paste full contents of:
`src/profiles/mb/data/front_matter/032025olsen/cover.json`

---

## Section 5 — Court case cover JSON for comparison

Paste full contents of:
`src/profiles/mb/data/front_matter/halprin/cover.json`

Identify field-by-field differences vs Olsen. Where Olsen has fields Halprin doesn't, or vice versa. Where shared fields have structurally different shapes (e.g., court_header_lines length).

---

## Section 6 — Appearances page anchoring check

Per the renderer gaps capture: B1.7.4 likely also affects appearances page anchoring for arbitration. Investigate:

- Read `src/stage5/front_matter/appearances_renderer.py`.
- Identify any code that depends on cover layout (page number anchoring, slot offsets, post-cover positioning).
- Compare Olsen appearances oracle (page 2 from Section 2) vs Halprin appearances structure.
- Report: does Olsen appearances structure differ from court-case appearances? If yes, how?

---

## Section 7 — Proposed slot layout for arbitration cover

Based on Sections 2-6, propose the layout structure for arbitration cover. Not code yet — design.

- How many vertical slots on page 1?
- Where does the single-line court_header sit?
- Where does the "IN THE MATTER OF..." block sit?
- Where does the witness block sit?
- Where does the date block sit?
- Where does the "DEPOSITION OF..." anchor land?
- What changes in `_DEPO_START_IDX` to support count=1?

If multiple valid layouts exist, list them with tradeoffs. Recommend one.

---

## Section 8 — Files that B1.7.4 build will touch

Based on the recon, list every file the eventual build will touch and why. Best estimate. Include any files outside `cover.py` that need changes.

---

## Section 9 — Collision check with B1.7.3

Sonnet #2 is building B1.7.3 in parallel on `src/stage5/front_matter/stipulation.py`. Confirm:

- Does B1.7.4 need to touch `src/stage5/front_matter/stipulation.py`? Expected answer: no.
- Does B1.7.4 need to touch any file B1.7.3 touches? Expected answer: no.
- If yes to either, flag immediately to Opus.

---

## Section 10 — Open questions for Opus

List anything ambiguous or needing a call before the B1.7.4 build spec is written.

---

## Files Touched

| File | Why |
|---|---|
| mrx-context/knowledge/2026-05-16_OLSEN_ARBITRATION_COVER_RECON.md | New recon capture (only deliverable) |

No engine code touched. No JSON touched. Read-only recon.

---

## Prime Directive Check

Could this reduce transcript accuracy or credibility?
- Recon is read-only. No code changes. No transcript output changes. Risk: zero this pass.
- Risk shifts to the future B1.7.4 build spec — which will get its own Prime Directive check before any code lands.

---

## Rollout Sequence

1. Sonnet #1 cuts feature/b1.7.4-recon from origin/main after git fetch.
2. Sonnet #1 produces the recon capture at the path above.
3. Sonnet #1 commits the markdown file. Pushes branch.
4. Scott pushes commit when asked.
5. Opus reads the recon, writes B1.7.4 build spec tomorrow.

30-minute wall per recon pass — extend with Scott approval if needed; this is meaty.

---

## Coordination With Sonnet #2 (Lane A)

- Sonnet #2 is on B1.7.3 BUILD (waive-mode stipulation). Owns `src/stage5/front_matter/stipulation.py`.
- DO NOT touch: `src/stage5/front_matter/stipulation.py`.
- Sonnet #2 will not touch `src/stage5/front_matter/cover.py`.

---

## Rollback Plan

- Recon capture is a single markdown file. Delete it if needed.
- No engine changes to revert.

---

## Open Items Requiring Scott Sign-Off

- PII boundary in Section 2 — pasting Olsen oracle structure into public repo. Spec says use placeholders for names. If Scott wants stricter handling (e.g., recon stays local only), Sonnet #1 surfaces this in Section 10 before committing.

— End of spec —

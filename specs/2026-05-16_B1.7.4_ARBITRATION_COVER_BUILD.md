# B1.7.4 — Arbitration Cover Layout — BUILD

**Date:** 2026-05-16
**Author:** Opus (architect)
**Builder:** Sonnet #1 (Lane B)
**Branch:** feature/b1.7.4-arbitration-cover (cut FRESH from origin/main after git fetch — NOT from feature/b1.7.4-recon)
**Predecessor:** specs/2026-05-16_B1.7.4_RECON_ARBITRATION_COVER.md + knowledge/2026-05-16_OLSEN_ARBITRATION_COVER_RECON.md

---

## Header (per RULE_SHEET_v1)

- Universal? YES (arbitration cover; serves all reporters with arbitration matters)
- Code or prompt? CODE
- File location: src/stage5/front_matter/cover.py
- Commit prefix: universal:

---

## Purpose

Implement the count=1 + loc_len=6 cover layout variant. Current renderer crashes for Olsen at `_DEPO_START_IDX.get(1)` returning None. Recon confirmed two changes unblock the depo cleanly. No other engine files need modification.

After this fix: Olsen renders end-to-end via the harness. All 6 MB depos render (pending B1.7.3 also landing for waive-mode).

---

## Opus Answers to Recon Open Questions

For builder reference. No action needed from builder on Q1, Q3, Q4.

- **Q1 (backtick artifact):** Ignore. JSON has no backtick; renderer produces no backtick.
- **Q2 (reporter anchor 18 vs 19):** Out of scope for B1.7.4. Accept the 1-line vertical delta as expected divergence. Documented in test plan below. Separate tile filed as B1.7.6.
- **Q3 (label_in_main semantics):** Leave as-is. Generalization deferred to CR #2 onboarding.
- **Q4 (loc_len=6 universality):** Build only loc_len=6 for count=1. Existing else-clause already raises NotImplementedError for unhandled combinations — no new guard needed.
- **Q5 (Olsen index page count):** Verified. build_index_pages emits 2 pages. Cover fix is sufficient for correct page numbering.

---

## RULE-TEST-CASE-FIRST — Test Cases

Builder confirms test cases in build report BEFORE writing code.

**Test Case 1 — Olsen renders without crash.**
- Run: `python io/analysis/run_front_matter.py --depo 032025olsen`
- Expected: exit code 0. Output file at `io/analysis/032025olsen/_front_matter_out/032025olsen.front_matter.txt`.
- Expected: cover page present in output. Page count includes cover + 2 index + appearances + stipulation pages (back matter skipped per Olsen's witness_reserves_signature=false until B1.7.3 also lands).

**Test Case 2 — Olsen cover SHAPE MATCHES oracle page 1.**
- SHAPE MATCH ONLY per Rule 7. Never byte-match.
- Compare to oracle structure documented in recon Section 2:
  - 1 court header line at slot 1
  - Caption block: 19 left lines + 2 right lines, paired into slots 2-11
  - Star + gap at slot 14 (sub row)
  - Depo block at slots 16-23 (8 slots)
  - Trailing blanks at slots 24-25
- Verify witness, taken_on, commencing, all 6 location lines, reporter line all appear in correct vertical positions.

**Test Case 3 — Appearances reporter line delta documented, not blocking.**
- Expected: Olsen appearances will render with reporter at line 19 (code anchor), not line 18 (oracle). This is a known 1-line vertical drift per Q2.
- Confirm this is the ONLY appearances delta. If anything else diverges, flag to Opus before commit.

**Test Case 4 — Halprin no regression.**
- Run: `python io/analysis/run_front_matter.py --depo halprin`
- Expected: output byte-identical to pre-change harness output.

**Test Case 5 — Williams no regression.**
- Run: `python io/analysis/run_front_matter.py --depo 060122williams`
- Expected: output byte-identical to pre-change harness output.

**Test Case 6 — Stipulation crash still present for Olsen.**
- Olsen has witness_reserves_signature=false. Until B1.7.3 also lands, the stipulation renderer will still raise NotImplementedError. Confirm this is the ONLY remaining crash for Olsen — i.e., the cover fix worked and stipulation is the next blocker.
- After B1.7.3 lands on main, re-run Test Case 1 to confirm Olsen renders fully end-to-end.

---

## Files Touched

| File | Why |
|---|---|
| src/stage5/front_matter/cover.py | Add `1: 15` to `_DEPO_START_IDX`; add `elif label_in_main and loc_len == 6:` branch |

One file. Two changes. Total estimated diff: ~15 lines added, 1 line modified.

---

## Implementation — Exact Changes

### Change 1 — Update `_DEPO_START_IDX` (line 31)

**Current:**
```python
_DEPO_START_IDX = {2: 12, 3: 10}
```

**New:**
```python
_DEPO_START_IDX = {1: 15, 2: 12, 3: 10}
```

### Change 2 — Add new depo block branch

Insert AFTER the existing `elif label_in_main and loc_len == 3:` branch and BEFORE the final else / NotImplementedError. Exact placement: builder confirms in build report by pasting the surrounding code with the new branch inserted.

**New branch (paste verbatim):**
```python
elif label_in_main and loc_len == 6:
    # 1-header arbitration case, 6-item location (Olsen-style)
    # 6-item loc pairs into 3 main/sub slots → ends in sub → reporter/STAR in sub
    slots.append((_c(depo_label, 55), _c("OF", 69)))
    slots.append((_c(witness, 57), ""))
    slots.append((_c(taken_on, 57), _c(commencing_str, 69)))
    slots.append((_c(location_lines[0], 57), _c(location_lines[1], 69)))
    slots.append((_c(location_lines[2], 57), _c(location_lines[3], 69)))
    slots.append((_c(location_lines[4], 57), _c(location_lines[5], 69)))
    slots.append(("", _c(reporter_str, 69)))
    slots.append(("", _STAR_SUB))
    # 8 slots: indices 15-22
```

No other code changes. Caption rendering and gap/star handling are already universal — they work for count=1 without modification.

---

## Prime Directive Check

Could this reduce transcript accuracy or credibility?

- **Risk 1: New branch produces wrong layout for Olsen.** MITIGATION: SHAPE MATCH against oracle in Test Case 2. Slot-by-slot verification per recon Section 7. Scott reviews rendered output before merge.
- **Risk 2: Halprin or Williams regress because `_DEPO_START_IDX` dict change cascades.** MITIGATION: Dict lookup is keyed on `num_headers` (1, 2, 3). Adding key 1 cannot affect lookups for 2 or 3. Test Cases 4 + 5 catch any regression.
- **Risk 3: Future arbitration depo has loc_len ≠ 6, silently mis-renders.** MITIGATION: Existing final else-clause in the depo block dispatcher already raises NotImplementedError for any unhandled (label_in_main, loc_len) combination. Loud failure, not silent. No new guard needed.
- **Risk 4: Appearances reporter anchor at line 19 vs oracle line 18 looks wrong to MB.** MITIGATION: Documented divergence; B1.7.6 filed for proper fix. Scott aware before merge.

All risks mitigated.

---

## Rollout Sequence

1. Sonnet #1 cuts feature/b1.7.4-arbitration-cover from origin/main after `git fetch`. Confirms branch + ancestry in build report.
   - `git fetch origin main`
   - `git checkout -b feature/b1.7.4-arbitration-cover origin/main`
   - `git branch --show-current` → must show feature/b1.7.4-arbitration-cover
   - `git log origin/main..HEAD --oneline` → must be empty
2. Sonnet #1 implements Change 1 + Change 2 in `src/stage5/front_matter/cover.py`.
3. Sonnet #1 runs Test Cases 1-6.
4. Sonnet #1 posts build report:
   - Branch verification output.
   - Diff of cover.py changes (paste the new branch in context).
   - Test Case 1-6 results.
   - Test Case 2 shape comparison (slot-by-slot vs oracle).
   - Confirms Test Case 3 — appearances delta is the 1-line reporter anchor only.
5. STOP. Wait for Opus to review diff + test results.
6. On Opus go: commit one logical commit (`universal: B1.7.4 add arbitration cover layout (count=1, loc_len=6)`). Push branch.
7. Push relaxed for tonight — Sonnet pushes directly.

30-minute wall per build pass. Backup branch before any cleanup.

---

## Coordination With Sonnet #2 (Lane A)

- Sonnet #2 owns `src/stage5/front_matter/stipulation.py` via B1.7.3 build.
- DO NOT touch: `src/stage5/front_matter/stipulation.py`.
- Sonnet #2 will not touch `src/stage5/front_matter/cover.py`.

Olsen full end-to-end render requires BOTH B1.7.3 (waive stipulation) and B1.7.4 (cover) to land. Order of merge doesn't matter — each fixes a different crash point on the Olsen pipeline.

---

## Rollback Plan

- One file changed. Revert the commit if Test Cases fail post-merge.
- Branch isolated. Abandoning is cheap.

---

## Open Items Requiring Scott Sign-Off

None at spec time. Five recon open questions resolved above.

— End of spec —

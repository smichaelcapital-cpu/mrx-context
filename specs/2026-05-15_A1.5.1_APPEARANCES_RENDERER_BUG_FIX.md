=== A1.5.1 SPEC — APPEARANCES RENDERER BUG FIX ===
Date: 2026-05-15
Author: Opus (architect)
Builder: Sonnet #1
Branch: feature/appearances-renderer-fix
  (cut FROM feature/appearances-wire-in, NOT from main)

RULE SHEET HEADER:
- Universal? YES (renderer applies to every CR)
- Code or prompt? CODE
- File location: C:\Users\scott\OneDrive\Documents\mrx_engine_v1\
  mrx_engine_v1\src\stage5\appearances_renderer.py
- Spec file: C:\Users\scott\OneDrive\Documents\mrx-context\specs\
  2026-05-15_A1.5.1_APPEARANCES_RENDERER_BUG_FIX.md
- Commit prefix: universal:

RULE-SPEC-SAVED-FIRST: save this spec to the path above BEFORE
any code change.

PRIME DIRECTIVE CHECK:
Could this change reduce transcript accuracy or credibility?
YES — this fix touches transcript content. Bug A is engine altering
MB's input (two spaces where she typed a middle dot). Bug B is
engine producing wrong indentation. Both reduce credibility against
oracle. Mitigation: byte-match acceptance against MB oracle FINAL.

WHY THIS EXISTS:
A1.5 wire-in is mechanically correct but Halprin does not
byte-match because appearances_renderer.py has two pre-existing
bugs surfaced by the wire-in test:

BUG A — Middle dot replaced with two spaces
- Halprin page 5, line 32
- Oracle: "Houston, Texas· 77002-5096"  (U+00B7 middle dot)
- Ours:   "Houston, Texas  77002-5096"  (two ASCII spaces)
- This is RULE-INPUT-IS-SACRED territory. Renderer is altering
  input. Defect, not cosmetics.

BUG B — Indentation off by 1 space
- Halprin pages 6, 7, 8, 9, 10
- Specific lines (from Sonnet #1's diff table):
  page 6 line 10, page 7 line 9, page 8 line 8, page 9 line 9,
  page 10 line 10
- Systematic 1-space shift across 5 pages
- Likely a config constant or template literal off by one space

OUT OF SCOPE:
- Williams byte-match test failure is PRE-EXISTING and a separate
  spec. Do NOT fix render_appearances_from_json in this branch.
- All other front matter blocks
- assemble_final.py (already correct from A1.5)
- appearances_page.py (legacy fallback, untouched)

STEP 1 — RECON (do this first, report before any code change):
1. cd C:\Users\scott\OneDrive\Documents\mrx_engine_v1\mrx_engine_v1\
2. git status — confirm clean working tree
3. git checkout feature/appearances-wire-in
4. git checkout -b feature/appearances-renderer-fix
5. Confirm with: git branch --show-current
   (must be feature/appearances-renderer-fix)
6. Read src\stage5\appearances_renderer.py end to end
7. Locate exactly where Bug A is introduced:
   - Search for the address string handling
   - Find where Houston/Texas/zip is composed
   - Is the middle dot in the JSON data and the renderer is
     dropping it? Or is the renderer adding two spaces and the
     JSON never had the dot? READ BOTH SOURCES.
   - Check the actual JSON content:
     type src\profiles\mb\data\front_matter\halprin\appearances.json
     | findstr /C:"77002"
   - Report what the JSON contains for that line, byte for byte
8. Locate exactly where Bug B is introduced:
   - Search for indent constants, leading-space patterns
   - Compare page 5 (working) vs pages 6-10 (off by 1) — what
     differs in the code path between them?
   - Most likely a template literal with one less space than
     needed, or a section header indent constant
9. STOP. Report findings:
   - For Bug A: is the middle dot in JSON? Where is the renderer
     dropping/replacing it? Line number(s)?
   - For Bug B: what's the off-by-one source? Line number(s)?
   - Proposed fix for each — but DO NOT edit yet

WAIT FOR SCOTT + OPUS GO-BUILD.

STEP 2 — FIX (only after recon approval):

Bug A fix:
- If JSON has the middle dot: renderer should pass it through
  verbatim (RULE-INPUT-IS-SACRED). Find where it's dropped or
  re-encoded and fix.
- If JSON does NOT have the middle dot: that's a data bug, not
  a renderer bug. Report and STOP — separate spec needed to
  fix halprin.json. Do not invent dots.

Bug B fix:
- Edit the indent constant or template literal to match oracle
- The change should be a single-character or 1-line fix
- If it requires touching multiple constants, ping Scott BEFORE
  making changes

STEP 3 — TEST:
1. pytest tests/stage5/ -v
   - All tests must pass EXCEPT test_williams_byte_match (known
     pre-existing failure, leave alone)
2. Re-render Halprin via _run_halprin_full_stage5.py
3. Diff vs oracle:
   C:\Users\scott\OneDrive\Documents\mrx-context\oracle\finals\
   halprin\040226yellowrock-FINAL.txt
4. Diff vs .pre-fix.txt baseline
5. Report byte-match results

ACCEPTANCE CRITERIA:
1. Branch feature/appearances-renderer-fix
2. ONLY appearances_renderer.py edited (no other file)
3. All stage5 tests pass except known pre-existing Williams failure
4. Halprin byte-matches oracle FINAL — exact, zero diff
5. Halprin byte-matches .pre-fix.txt baseline — exact, zero diff
6. test_23_appearances_pages_match_mb_final now PASSES

ROLLBACK PLAN:
- Branch cut from feature/appearances-wire-in (the wire-in is safe)
- If renderer fix fails: abandon this branch, wire-in branch is
  still intact for a different fix attempt
- Main untouched throughout

PRINT BEFORE COMMIT:
type src\stage5\appearances_renderer.py
type src\profiles\mb\data\front_matter\halprin\appearances.json
(both — second one only if JSON ended up changed; if not, just print
renderer for the visible backup)

DELIVERABLES:
1. Spec file saved to specs/ (per RULE-SPEC-SAVED-FIRST)
2. Step 1 recon report
3. Fix on feature/appearances-renderer-fix
4. Halprin byte-match against oracle (must pass)
5. Halprin byte-match against .pre-fix (must pass)
6. Brief delta report

30-MINUTE WALL per step. Report partial if blocked.

DO NOT PUSH — branch local until Scott approves merge plan.

MERGE PLAN (Opus + Scott decide after byte-match passes):
Option A: merge renderer-fix into appearances-wire-in, then merge
  combined into main as one commit
Option B: merge renderer-fix to main, then merge wire-in to main
  (two commits)
Recommend Option A — cleaner story in main's log.

LEDGER UPDATE (Opus owns after fix lands):
- Row 19 status: DEBT → RESOLVED (full wire-in + renderer fix)
- New row: Bug A and Bug B pre-existing renderer defects fixed
  2026-05-15

=== END A1.5.1 SPEC ===

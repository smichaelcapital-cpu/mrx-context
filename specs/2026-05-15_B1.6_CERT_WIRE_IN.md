=== B1.6 SPEC — WIRE IN REPORTER CERT RENDERER ===
Date: 2026-05-15
Author: Opus (architect)
Builder: Sonnet #2
Branch: feature/cert-wire-in (cut from main)

RULE SHEET HEADER:
- Universal? YES (cert renderer applies to every CR)
- Code or prompt? CODE
- File location: C:\Users\scott\OneDrive\Documents\mrx_engine_v1\
  mrx_engine_v1\src\stage5\
- Spec file: C:\Users\scott\OneDrive\Documents\mrx-context\specs\
  2026-05-15_B1.6_CERT_WIRE_IN.md
- Commit prefix: universal:

RULE-SPEC-SAVED-FIRST: save this spec to the path above BEFORE
any code change.

PRIME DIRECTIVE CHECK:
Could this change reduce transcript accuracy or credibility?
YES — cert pages are legally required content. CCR number, signature
block, witness errata link all live there. If the renderer drops or
mis-orders content, the depo is legally defective. Mitigation:
byte-match acceptance test against Halprin oracle FINAL. Renderer
ships only if byte-match holds.

WHY THIS EXISTS:
Per B1.5 audit (Sonnet #2, 2026-05-15): cert renderer
(build_back_matter in orchestrator.py) is built and data-driven.
Two JSON files exist and are correct for Halprin. BUT
build_back_matter() is called from nowhere. The cert pages
currently in Halprin output (pages 296-300) come through the body
paginator from the steno source — NOT from the renderer. Same
pre-wire-in state appearances was in before A1.5.

This spec wires it in. Same pattern A1.5 follows.

PARALLEL WORK NOTE:
A1.5 (appearances wire-in, Sonnet #1) is happening in parallel.
Both lanes touch assemble_final.py. Sonnet #2 must check for
conflicts at Step 2 before editing.

STEP 1 — RECON (do this first, report before any code change):
1. cd C:\Users\scott\OneDrive\Documents\mrx_engine_v1\mrx_engine_v1\
2. git status — confirm clean working tree, currently on main
3. git checkout -b feature/cert-wire-in
4. Read these files end to end and report a one-paragraph
   summary of each:
   a. src\stage5\assemble_final.py — find where body_doc is
      assembled and where cert pages currently come from
   b. src\stage5\orchestrator.py — read build_back_matter()
      function (its signature, args, return type)
   c. src\stage5\reporter_cert.py — read the renderer module
   d. src\stage5\witness_cert.py — read the renderer module
   e. src\stage5\data\<cert json files> — confirm both exist,
      report exact paths and sizes
5. Report:
   - Exact location in assemble_final.py where cert content
     currently appears (which lines from body_doc become
     pages 296-300?)
   - build_back_matter() signature and return type
   - Does the return type match what assemble_final.py needs to
     append? If not, what's the gap?
   - The two hardcoded MB-name lines flagged in B1.5 audit
     (reporter_cert.py line 7-8, witness_cert.py _REPORTER_LINE)
     — confirm exact line numbers
6. STOP. Post recon. Wait for Scott + Opus go-build.

STEP 2 (only after Scott approves recon):
1. Check git log on main and feature/appearances-wire-in (A1.5's
   branch). If A1.5 has merged to main, pull latest before
   editing. If A1.5 has NOT merged, edit on top of main directly.
   Report which case applies.

2. In assemble_final.py:
   a. Strip cert lines from body_doc before layout (recon will
      surface the exact strip pattern — likely a line-range or
      marker-based slice)
   b. Call build_back_matter(depo_stem) AFTER body layout
      completes
   c. Append renderer output to final document

3. The old cert-from-body path stays on disk for rollback (do
   NOT delete any cert .py files).

4. Run existing test suite: pytest tests/stage5/ -v
   - All existing tests must pass

5. Run Halprin byte-match test:
   - Re-render full Halprin via _run_halprin_full_stage5.py
   - Diff output against MB FINAL oracle:
     C:\Users\scott\OneDrive\Documents\mrx-context\oracle\finals\
     halprin\040226yellowrock-FINAL.txt
   - Diff output against the .pre-fix.txt baseline
   - Report: byte-match result for both diffs

ACCEPTANCE CRITERIA:
1. Branch feature/cert-wire-in created, clean diff
2. assemble_final.py is the ONLY pipeline change (no renderer
   logic edits)
3. All existing stage5 tests pass
4. Halprin output byte-matches MB oracle FINAL
5. Halprin output byte-matches the pre-wire-in baseline (.pre-fix)
   for cert pages (last 5 pages)
6. Body of depo unchanged — only the last 5 pages should differ
   in source path (renderer vs body paginator), output should
   be identical

ROLLBACK PLAN:
- Branch isolated from main
- All cert .py modules left on disk
- If byte-match fails: revert assemble_final.py change, branch
  abandoned, main untouched

OUT OF SCOPE — do not touch:
- reporter_cert.py internals (already shipped)
- witness_cert.py internals (already shipped)
- The two hardcoded MB-name lines (separate ledger row, fixable
  when CR #2 onboards — NOT today)
- Any cert JSON data files
- Anything Sonnet #1 is touching in A1.5
- Appearances code (A1.5's lane)

PRINT BEFORE COMMIT:
type src\stage5\assemble_final.py
type src\stage5\orchestrator.py

DELIVERABLES:
1. Spec file saved to specs/ (per RULE-SPEC-SAVED-FIRST)
2. Recon report (Step 1)
3. Wire-in code change on feature/cert-wire-in branch
4. Halprin byte-match result against oracle
5. Halprin byte-match result against pre-fix baseline
6. Brief delta report

30-MINUTE WALL on each step. Report partial progress if blocked.

DO NOT PUSH — branch stays local until Scott approves merge.

LEDGER UPDATE (Opus owns after wire-in confirms):
1. Add row for cert wire-in resolution
2. Add row for the two MB-name hardcodes as KNOWN DEBT for
   CR #2 onboarding (10-minute fix later, not today)

=== END B1.6 SPEC ===

=== A1.5 SPEC — WIRE IN APPEARANCES RENDERER ===
Date: 2026-05-15
Author: Opus (architect)
Builder: Sonnet #1
Branch: feature/appearances-wire-in (cut from main)

RULE SHEET HEADER:
- Universal? YES (renderer applies to every CR's appearances page)
- Code or prompt? CODE
- File location: C:\Users\scott\OneDrive\Documents\mrx_engine_v1\
  mrx_engine_v1\src\stage5\
- Spec file: C:\Users\scott\OneDrive\Documents\mrx-context\specs\
  2026-05-15_A1.5_APPEARANCES_WIRE_IN.md
- Commit prefix: universal:

RULE-SPEC-SAVED-FIRST: save this spec to the path above BEFORE
any code change.

PRIME DIRECTIVE CHECK:
Could this change reduce transcript accuracy or credibility?
YES — if wired in wrong, MB's appearances page could drop attorneys,
mis-attribute firms, or break the byte-match against her oracle.
Mitigation: byte-match acceptance test against Halprin oracle.
Renderer ships only if byte-match holds.

WHY THIS EXISTS:
Last Sunday-Monday we built the data-driven appearances renderer
(appearances_renderer.py) and hand-extracted halprin.json (22 firm
groups). Thursday's KEEP-TOGETHER + "Reported by:" line-19 fixes
landed in that renderer. BUT the live pipeline still calls the old
hardcoded appearances_page.py. So the renderer is correct code that
isn't running. This spec wires it in.

After wire-in, Halprin must still byte-match. Then we test on
Brandl (or whichever second depo has a hand-extracted JSON ready)
to prove the generalization actually works.

STEP 1 — RECON (do this first, report before any code change):
1. cd C:\Users\scott\OneDrive\Documents\mrx_engine_v1\mrx_engine_v1\
2. git status — confirm clean working tree, currently on main
3. git checkout -b feature/appearances-wire-in
4. Read these files end to end and report a one-paragraph
   summary of each:
   a. src\stage5\assemble_final.py (where appearances_page is
      called — find the call site)
   b. src\stage5\appearances_page.py (the hardcoded template
      that drives live output)
   c. src\stage5\appearances_renderer.py (the data-driven
      renderer Sonnet hand-built last week)
   d. src\stage5\data\appearances\halprin.json (the data input
      for the renderer)
5. Report:
   - Exact call site in assemble_final.py (file + line + function)
   - Signature of appearances_renderer's entry function
     (what args does it take, what does it return?)
   - Does the renderer's return type match what assemble_final.py
     expects from appearances_page? If not, what's the gap?
   - Is there a williams.json (or any second depo) in
     src\stage5\data\appearances\? If yes, name it.
6. STOP. Post recon. Wait for Scott + Opus go-build.

STEP 2 (only after Scott approves recon):
1. In assemble_final.py, swap the call:
   - REMOVE the call to appearances_page (the hardcoded path)
   - ADD a call to appearances_renderer with the appropriate
     args (data path or loaded JSON, whatever signature recon
     surfaced)
2. The old appearances_page.py file stays on disk for now (do
   NOT delete) — it's our rollback if byte-match fails.
3. Run existing test suite: pytest tests/stage5/ -v
   - All existing tests must pass
4. Run Halprin byte-match test:
   - Re-render full Halprin via the same Stage 5 runner used
     this morning (_run_halprin_full_stage5.py)
   - Diff output against the existing .pre-fix.txt baseline AND
     against the MB FINAL oracle:
     C:\Users\scott\OneDrive\Documents\mrx-context\oracle\finals\
     halprin\040226yellowrock-FINAL.txt
   - Report: byte-match result for both diffs

STEP 3 — SECOND DEPO TEST (only if Step 2 byte-matches Halprin):
1. Check src\stage5\data\appearances\ for a second JSON
   (williams.json or any other).
2. If a second JSON exists:
   a. Identify which depo it represents
   b. Render that depo's appearances using the wired-in renderer
   c. Output only the appearances pages (not full depo) to:
      C:\Users\scott\OneDrive\Documents\mrx-context\renders\
      2026-05-15_<depo>_appearances_test.txt
   d. Find that depo's MB oracle FINAL file and diff
   e. Report byte-match result + any deltas
3. If NO second JSON exists yet:
   a. Report which depos have FINAL.txt oracles available
      (Brandl, Easley, Cavazos, etc.)
   b. STOP and tell Scott — second JSON extraction is a
      separate spec, not this one

ACCEPTANCE CRITERIA:
1. Branch feature/appearances-wire-in created, clean diff
2. assemble_final.py swap is the ONLY pipeline change (no
   renderer logic edits)
3. All existing stage5 tests pass
4. Halprin output byte-matches MB oracle FINAL
5. Halprin output byte-matches the pre-wire-in baseline
   (.pre-fix.txt) — proves the renderer is producing identical
   output to the hardcoded template for Halprin
6. If a second JSON exists: that depo's appearances byte-matches
   its oracle too

ROLLBACK PLAN:
- Branch isolated from main
- appearances_page.py left in place
- If byte-match fails: revert assemble_final.py change, branch
  abandoned, main untouched

OUT OF SCOPE — do not touch:
- appearances_renderer.py internals (already shipped, working)
- appearances_page.py (leave it on disk as fallback)
- halprin.json (already hand-extracted, do not edit)
- Reporter Certificate (separate spec — B1.5 coming)
- Other front matter blocks (cover/stipulation/videographer
  already wired in correctly)

PRINT BEFORE COMMIT:
type src\stage5\assemble_final.py
type src\stage5\appearances_renderer.py

DELIVERABLES:
1. Spec file saved to specs/ (per RULE-SPEC-SAVED-FIRST)
2. Recon report (Step 1)
3. Wire-in code change on feature/appearances-wire-in branch
4. Halprin byte-match result
5. Second depo byte-match result (if JSON exists)
6. Brief delta report

30-MINUTE WALL on each step. Report partial progress if blocked.

DO NOT PUSH — branch stays local until Scott approves merge.

LEDGER UPDATE (Opus owns after wire-in confirms):
Update row 16 status from "DEBT" to "RESOLVED — wired in on
2026-05-15". Note byte-match results.

=== END A1.5 SPEC ===

=== A1.5 SPEC v2 — WIRE IN APPEARANCES RENDERER (AMENDED) ===
Date: 2026-05-15
Author: Opus (architect)
Builder: Sonnet #1 (fresh agent, 10AM session)
Branch: feature/appearances-wire-in (cut from main, confirmed)

REPLACES: 2026-05-15_A1.5_APPEARANCES_WIRE_IN.md (v1)
v1 had two gaps Sonnet #1's first recon surfaced. This v2 closes them.

RULE SHEET HEADER:
- Universal? YES (renderer applies to every CR's appearances page)
- Code or prompt? CODE
- File location: C:\Users\scott\OneDrive\Documents\mrx_engine_v1\
  mrx_engine_v1\src\stage5\
- Spec file: C:\Users\scott\OneDrive\Documents\mrx-context\specs\
  2026-05-15_A1.5_APPEARANCES_WIRE_IN_v2.md
- Commit prefix: universal:

RULE-SPEC-SAVED-FIRST: save this amended spec to the path above
BEFORE any code change. Leave v1 in place — do not delete.

PRIME DIRECTIVE CHECK:
Could this change reduce transcript accuracy or credibility?
YES — wrong stitch or wrong page count breaks the byte-match
against MB's oracle. Mitigation: byte-match acceptance test
against Halprin oracle FINAL. Ships only if byte-match holds.

WHAT v1 GOT WRONG:
1. JSON path. v1 said src/stage5/data/appearances/halprin.json.
   Real path: src/profiles/mb/data/front_matter/halprin/appearances.json
2. v1 assumed a single-call swap. Reality: the renderer only
   covers pages 5-10. Pages 2-4 (Index + Exhibits) are still
   hardcoded in appearances_page.py as _PAGE_2, _PAGE_3, _PAGE_4
   and must be stitched in.

CORRECTED APPROACH:

STEP 1 — RECON (confirm before any code change):
Previous Sonnet #1 already did this. Fresh Sonnet #1 has read
the handoff. Confirm by reporting:
   a. assemble_final.py call site for appearances_page (file + line)
   b. appearances_renderer.build_appearances_pages_v2 signature
   c. Exact existence of _PAGE_2, _PAGE_3, _PAGE_4 string constants
      in appearances_page.py (line numbers)
   d. Exact path of halprin appearances JSON:
      C:\Users\scott\OneDrive\Documents\mrx_engine_v1\mrx_engine_v1\
      src\profiles\mb\data\front_matter\halprin\appearances.json
      Confirm file exists.

STOP. Report findings. Wait for Scott go-build.

STEP 2 — WIRE IN (only after Scott approves Step 1):

In assemble_final.py:

a. KEEP the call to appearances_page.py for pages 2-4. Specifically
   import or reference _PAGE_2, _PAGE_3, _PAGE_4 as before.

b. ADD a call to build_appearances_pages_v2 using the JSON at
   src/profiles/mb/data/front_matter/halprin/appearances.json.
   (Use case_info.get("depo_id", "halprin") to resolve depo_stem,
   then build the path from depo_stem. Same pattern B1.6 uses for
   cert.)

c. STITCH: concatenate _PAGE_2 + _PAGE_3 + _PAGE_4 + renderer_output
   in that order. This produces pages 2-10 of appearances.

d. UPDATE the page count tracker: _appearances_pages = 3 +
   renderer_page_count. For Halprin: 3 + 6 = 9. (Same as today's
   value — if it's not 9 after the swap, something is wrong.)

e. The old appearances_page.py file STAYS on disk for now (do NOT
   delete) — it's our rollback if byte-match fails.

f. appearances_renderer.py — do NOT edit. It already works.

STEP 3 — TEST:
1. Run existing test suite: pytest tests/stage5/ -v
   - All existing tests must pass
2. Run Halprin byte-match test:
   - Re-render full Halprin via _run_halprin_full_stage5.py
   - Diff output against MB FINAL oracle:
     C:\Users\scott\OneDrive\Documents\mrx-context\oracle\finals\
     halprin\040226yellowrock-FINAL.txt
   - Diff output against .pre-fix.txt baseline created earlier today
   - Report: byte-match result for both diffs

STEP 4 — SECOND DEPO TEST (only if Step 3 byte-matches Halprin):
Five other depo JSONs are confirmed to exist:
  - src\profiles\mb\data\front_matter\032025olsen\appearances.json
  - src\profiles\mb\data\front_matter\0525black_bp\appearances.json
  - src\profiles\mb\data\front_matter\060122williams\appearances.json
  - src\profiles\mb\data\front_matter\082222butler\appearances.json
  - src\profiles\mb\data\front_matter\101322blanks\appearances.json

Pick ONE depo (recommend 060122williams — Sunday/Monday work was
done with williams in mind). Render only its appearances pages
(not full depo) using the wired-in renderer.

NOTE: The cover/stipulation/videographer renderers may or may not
have data for this depo. If they crash, that's a separate issue —
this test isolates the appearances renderer only.

a. Output to:
   C:\Users\scott\OneDrive\Documents\mrx-context\renders\
   2026-05-15_williams_appearances_test.txt
b. Find williams MB oracle FINAL (Sonnet #1 recons the path —
   check C:\mrx_training_set\MB\paired\060122williams\final\)
c. Diff and report byte-match result.
d. If oracle not found: stop, report, don't invent.

ACCEPTANCE CRITERIA:
1. Branch feature/appearances-wire-in, clean diff
2. assemble_final.py is the ONLY pipeline change
3. All stage5 tests pass
4. Halprin byte-matches oracle FINAL
5. Halprin byte-matches .pre-fix.txt baseline
6. Williams appearances byte-matches williams oracle
   (if oracle found — otherwise note and proceed)

ROLLBACK PLAN:
- Branch isolated from main
- appearances_page.py still on disk
- If byte-match fails: revert assemble_final.py, branch abandoned

OUT OF SCOPE — do not touch:
- appearances_renderer.py internals
- appearances_page.py (used as page 2-4 source — leave intact)
- Any JSON data file
- Reporter Certificate path (B1.6 is Sonnet #2's lane)
- All other front matter blocks

PRINT BEFORE COMMIT:
type src\stage5\assemble_final.py

DELIVERABLES:
1. v2 spec saved to specs/ (per RULE-SPEC-SAVED-FIRST)
2. Step 1 recon report (quick, mostly already done)
3. Wire-in code change on feature/appearances-wire-in
4. Halprin byte-match against oracle
5. Halprin byte-match against .pre-fix baseline
6. Williams byte-match against oracle (if oracle found)

30-MINUTE WALL on each step. Report partial.

DO NOT PUSH — Scott approves merge after byte-matches confirm.

LEDGER UPDATE (Opus owns after wire-in confirms):
- Update row 19 status: DEBT → RESOLVED on 2026-05-15
- Add row: known debt — premature witness_cert.json load in
  build_back_matter() (B1.6 audit found this — applies to
  future depos with read_and_sign=False)

=== END A1.5 SPEC v2 ===

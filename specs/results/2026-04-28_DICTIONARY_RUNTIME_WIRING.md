# Dictionary Runtime Wiring — 2026-04-28

Generated by: Sonnet (builder)
Spec: HANDOFF_SONNET_2026-04-28 — Spec 1

---

## Verdict

**NOT WIRED**

`dictionary_loader.py` is never called during a Halprin pipeline run.
The runner passes an empty dict `{}` to Stage 3. Even if a non-empty dict were passed,
the `if dictionary:` guard in the suggester skips the dictionary block when the value is falsy.
The `dictionary_hash=None` field in the run metadata confirms this state was known.

---

## Call Chain Evidence

### 1. Runner — `io/analysis/halprin_mini/_run_halprin_mini.py`

- **dictionary_loader imported?** NO. No `import` of `dictionary_loader` anywhere in file.
- **Dictionary loaded from any path?** NO. No path to `.rtf` dictionary referenced.
- **Dictionary passed to pipeline?** YES — but as `{}` (empty dict).
  - Line 267: `suggest_proposals(batch, client, {}, state_module, NAMES_LOCK)`
  - Third positional argument is the `dictionary` parameter.
- **Metadata confirms:** Line 147: `dictionary_hash=None` in `ProposalRunMetadata`.
- **Status: NOT WIRED** — runner never calls `dictionary_loader.load()`.

### 2. Stage 1 (RTF parse) — `src/mrx_engine_v1/stage1_rtf/pipeline.py`

- Runner does not invoke Stage 1 during a Stage 3.1 run. Runner reads Stage 2 output directly.
- **Status: NOT APPLICABLE** — Stage 1 is not called by this runner.

### 3. Stage 2 — `src/mrx_engine_v1/stage2/pipeline.py`

- Runner reads from `_stage2_out/halprin_mini.stage2.turns.json` directly (line 48).
- Stage 2 is not called during a Stage 3.1 run.
- **Status: NOT APPLICABLE** — Stage 2 is not called by this runner.

### 4. Stage 3 (LLM correction) — `src/mrx_engine_v1/stage3/suggester.py`

- `suggest_proposals(batch, api_client, dictionary, state_module, names_lock, config)` — line 659.
- `dictionary` parameter received as `{}` (empty dict, falsy).
- **Reader agent** (`_run_reader`, line 425):
  - Line 446–452: `if dictionary:` — evaluates `{}` as False → dictionary block **not added** to Reader system prompt.
- **Writer agent** (`_run_writer`, line 557):
  - Line 578–584: `if dictionary:` — same guard → dictionary block **not added** to Writer system prompt.
- **Status: LOADED BUT UNUSED** (infrastructure exists; value passed is empty).

### 5. Stages 4 and 5

- Runner (`_run_halprin_mini.py`) is a Stage 3.1 runner only. It does not call Stage 4 or Stage 5.
- Stage 5 has its own runner and case_info_loader. No dictionary parameter observed in Stage 5 schemas.
- **Status: NOT APPLICABLE** — dictionary not relevant to Stage 4/5 formatter.

---

## Runtime Verification

**SKIPPED** — static trace is unambiguous. The runner source code at line 267 shows a literal `{}` being passed as the dictionary. No ambiguity requires runtime confirmation. Adding a temporary print would require running the LLM pipeline (costs money, time). Static evidence is conclusive.

---

## Implication for B5 / B2 Defects

The NOT WIRED verdict means the LLM Reader and Writer agents have operated with no job-specific dictionary context in any Halprin run to date. However, wiring the job dictionary would not have fixed B2 ("under paid") or B5 ("with and T offshore"): neither term appears in any package file, including the job dictionary (`040226yellowrock-ROUGH.rtf`, confirmed by package inspection). Both defects arise from steno brief expansion behavior — the pipeline is generating text that does not exist in the source RTF, and the job dictionary covers only case-specific proper nouns (Saltsman, Cibulsky, etc.). B5 specifically stems from the steno stroke W-T expanding to "with" (by theory, not custom entry), with adjacent strokes producing "and T offshore" as a broken expansion — a Stage 1/2 artifact unrelated to dictionary lookup. B2 similarly reflects a steno brief for "underpaid" expanding to two tokens. Wiring the job dictionary is still correct housekeeping, but it will not resolve these two anomalies.

---

## Recommended Next Step

Wire the dictionary as a two-line fix in the runner: import `dictionary_loader`, call `load_dictionary` on `tests/fixtures/halprin_mini/dictionary.rtf` (or the package copy at `io/analysis/halprin_mini/package/040226yellowrock-ROUGH.rtf`), and pass the result to `suggest_proposals`. This is low-risk, confirms the infrastructure works end-to-end, and gives the LLM access to 116 case-specific translations. It will not fix B2 or B5 — those require a separate Stage 1/2 steno-expansion trace to find where the pipeline introduces text absent from the RTF. Opus should spec B5 and B2 as a pipeline-stages trace (Stage 1 RTF tokenizer through Stage 2 transforms) as the next investigation.

---

*Generated: 2026-04-28 | Sonnet builder | Engine repo not modified | Oracle Covenant observed*
